#!/usr/bin/env bash
set -euo pipefail

# Keep the SDK Sprite contract explicit even though the current preview only
# needs the tools supplied by the standard runtime.
mkdir -p /home/sprite/.sprite-dev
